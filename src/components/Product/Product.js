import React from 'react';

const Product = (props) => {
    console.log(props.product)
    return (
        <div>
            <h1>this is product</h1>
        </div>
    );
};

export default Product;